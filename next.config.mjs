/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ['heavyequipmenttransport.com', 'www.heavyequipmenttransport.com', 'www.tractortransport.com', 'ntslogistics.com'],
    },
};

export default nextConfig;